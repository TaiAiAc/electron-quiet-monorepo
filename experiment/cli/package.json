{
  "name": "cli",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "main": "index.js",
  "scripts": {
    "all": "npm run command && npm run dev && npm run dev:custom && npm run build && npm run build:custom",
    "command": "electronup -h && electronup -v",
    "dev": "electronup && electronup dev",
    "dev:custom": "electronup dev -c build/electronup.config.ts",
    "build": "electronup build",
    "build:custom": "electronup build -c build/electronup.config.ts"
  },
  "devDependencies": {
    "@quiteer/electronup": "workspace:*"
  }
}

{
  "name": "@quiteer/electronup",
  "version": "0.0.1",
  "description": "基于electron , ",
  "author": "quiteer",
  "license": "MIT",
  "keywords": [
    "electron"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "bin": {
    "electronup": "bin/index.js"
  },
  "files": [
    "bin"
  ],
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "test": "electronup -i index.js --input=index.js -p 3000 --port=3000 -m --minify -c=build.json --config=build.json -w --watch"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.2",
    "minimist": "^1.2.7",
    "zx": "^7.1.1"
  }
}
